# Security

All external traffic uses TLS. Agents authenticate with short-lived tokens. Customer email addresses are encrypted at rest and excluded from ordinary application logs.

Dummy security owner: `security-team`.
