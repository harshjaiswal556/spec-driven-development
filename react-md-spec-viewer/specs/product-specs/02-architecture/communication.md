# Communication

Frontend modules communicate through typed document metadata and selected document state.

The terminal, folder tree, search results, and internal Markdown links all update the same selected document.
