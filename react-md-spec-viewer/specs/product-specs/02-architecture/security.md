# Security

Markdown rendering must not execute arbitrary HTML or scripts from source documents.

External links should open safely if configured to open in a new tab. Terminal commands are navigation commands only and must not execute shell commands.
