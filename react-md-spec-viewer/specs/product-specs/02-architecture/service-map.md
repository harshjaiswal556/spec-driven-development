# Service Map

```text
content-ingestion -> navigation-shell -> markdown-preview
        |                    |
        v                    v
 search-index       interactive-experience
```

These are frontend modules, not backend services.
