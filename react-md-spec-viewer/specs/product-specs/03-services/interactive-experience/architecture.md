# Interactive Experience Architecture

The terminal panel should parse a small command vocabulary and update app state. It must never execute operating system commands.

The terminal panel must support a collapsed state controlled from its header. When expanded, terminal history should remain scrollable while hiding visible scrollbar chrome.
