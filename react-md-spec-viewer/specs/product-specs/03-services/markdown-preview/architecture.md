# Markdown Preview Architecture

The preview should render Markdown through a dedicated component. It should support headings, paragraphs, lists, tables, links, inline code, fenced code blocks, blockquotes, and frontmatter display or suppression based on product choice.
