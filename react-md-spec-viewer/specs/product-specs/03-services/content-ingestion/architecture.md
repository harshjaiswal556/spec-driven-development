# Content Ingestion Architecture

The app should use a manifest containing document paths, titles, categories, and loader references. The implementation may generate this manifest from the source Markdown tree during development or build.
