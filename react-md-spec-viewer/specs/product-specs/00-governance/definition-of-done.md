# Definition of Done

A feature is done when:

- it renders the required Markdown content
- navigation works with mouse and keyboard
- layout works on desktop and mobile widths
- Markdown tables, lists, links, and code blocks are readable
- tests cover core behavior
- no source content is silently omitted
