# AI Development Guidelines

AI implementation should retrieve the smallest relevant spec context, preserve the existing Markdown content as product data, and avoid rewriting source content unless a change request explicitly asks for it.

The UI should prioritize clarity over decoration. Do not create a marketing landing page; the first screen should be the usable spec browser.
