# JIRA Workflow

JIRA is out of scope for the first app build. The UI may display JIRA IDs found in Markdown as plain text or links if URL configuration is later provided.

No issue tracker integration is required for the initial React project.
