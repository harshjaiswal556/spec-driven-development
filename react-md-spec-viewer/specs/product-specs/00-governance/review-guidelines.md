# Review Guidelines

Reviewers should verify:

- all expected Markdown files are discoverable
- folder hierarchy matches the source tree
- rendered Markdown preserves meaning
- terminal commands do not expose unsafe actions
- keyboard navigation is usable
- visual design remains clean and simple
