# Spec Linting

Recommended checks:

- every feature folder has `spec.md`
- every feature spec has metadata
- acceptance criteria use stable IDs
- links to local Markdown files resolve
- no generated app spec claims code exists before implementation
- `.gitignore` exists at the React project root
- generated content such as `src/generated/docs.js` is reproducible and excluded from Git

