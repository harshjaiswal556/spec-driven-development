# ADR-002: Treat Markdown Files as Source Content

## Status

Proposed

## Context

The website exists to display the existing Markdown corpus, not replace it with rewritten web pages.

## Decision

Keep Markdown files as source content and render them through the app.

## Consequences

Content changes should happen in Markdown first. The app should avoid hardcoded copies of document bodies except for generated metadata.
