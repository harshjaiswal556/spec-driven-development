# ADR-001: Use React Single Page Application

## Status

Proposed

## Context

The product needs a fast, interactive UI with shared state between folder tree, Markdown preview, and terminal navigation.

## Decision

Use a React single page application.

## Consequences

The app can provide immediate interactions without server navigation. Build-time content indexing may be needed to include local Markdown files cleanly.
