# Local Markdown Integration

The first version integrates with local Markdown files bundled with or copied into the React project.

No remote CMS, database, or external account integration is required.
