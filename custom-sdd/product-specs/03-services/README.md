# Services

Services are the primary ownership boundary. Each service contains its architecture and long-lived feature folders.

JIRA tickets are linked from feature metadata and change history; they do not become permanent folders by default.
